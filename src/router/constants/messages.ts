/**
 * 路由相关的错误消息常量
 */

/**
 * 缓存配置错误：include 和 exclude 不能同时使用
 */
export const ERROR_CACHE_INCLUDE_EXCLUDE_MUTUAL = 'cache.include and cache.exclude cannot be used together. They are mutually exclusive.'

/**
 * 中间件错误：next() 不能被多次调用
 */
export const ERROR_NEXT_CALLED_MULTIPLE_TIMES = 'next() called multiple times'
